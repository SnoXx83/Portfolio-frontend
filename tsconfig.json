{
  "compilerOptions": {
    "target": "es2021",                         /* Cible ES2021 pour des fonctionnalités JS modernes. */
    "lib": ["dom", "dom.iterable", "esnext"],   /* Inclut les bibliothèques nécessaires pour le navigateur et les fonctionnalités ES modernes. */
    "allowJs": true,                            /* Autorise l'inclusion de fichiers JavaScript. */
    "skipLibCheck": true,                       /* Ignore la vérification des fichiers de déclaration des bibliothèques (souvent utile avec CRA). */
    "esModuleInterop": true,                    /* Active l'interopérabilité pour les modules CommonJS et ES. */
    "allowSyntheticDefaultImports": true,       /* Autorise les importations par défaut synthétiques. */
    "strict": true,                             /* Active toutes les options de vérification de type strictes. */
    "forceConsistentCasingInFileNames": true,   /* Force la cohérence de la casse des noms de fichiers. */
    "noEmit": true,                             /* N'émet pas de fichiers JavaScript compilés (laisse Webpack/Vite s'en occuper). */
    "resolveJsonModule": true,                  /* Permet d'importer des modules .json. */
    "isolatedModules": true,                    /* S'assure que chaque fichier peut être transpilé en toute sécurité de manière isolée. */
    "jsx": "react-jsx",                         /* Prend en charge JSX pour React (utilise le nouveau transform). */
    "moduleResolution": "node",                 /* Stratégie de résolution des modules (standard pour Node.js et les bundlers). */
    "baseUrl": "./",                            /* Définit le répertoire de base pour la résolution des modules non relatifs. */
    "paths": {                                  /* Mapper les importations pour des modules spécifiques (ex: "@components/*"). */
      "*": ["src/*"]
    },
    "typeRoots": ["./node_modules/@types", "./src/types"] /* Où chercher les fichiers de déclaration de type. */
  },
  "include": [
    "src"                                       /* Inclut tous les fichiers dans le dossier src. */
  ],
  "exclude": [
    "node_modules"                              /* Exclut le dossier node_modules de la vérification directe. */
  ]
}